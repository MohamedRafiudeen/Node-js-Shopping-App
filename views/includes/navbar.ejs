
<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="#">DudeShop</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon">-<br></span>
    </button>

  
    <div class="collapse navbar-collapse" id="navbarSupportedContent" style="display: flex;justify-content: space-around;">
      <ul class="navbar-nav mr-auto" style="min-width: 60%;">
        <li class="nav-item active">
          <a class="nav-link" href="/shop">Shop <span class="sr-only">(current)</span></a>
        </li>

        <div>
      <% if(isLoggedIn) { %>
        <li class="nav-item active">
            <a class="nav-link" href="/admin/add-products">Add-Product <span class="sr-only">(current)</span></a>
          </li>

          <li class="nav-item active">
            <a class="nav-link" href="#">Cart <span class="sr-only">(current)</span></a>
          </li>

          <li class="nav-item active">
            <a class="nav-link" href="#">my Orders <span class="sr-only">(current)</span></a>
          </li>
      <% } %>
        

        </ul>
      </div>

      
        <div class="r-nav">
          <ul class="navbar-nav mr-auto">
          <% if(!isLoggedIn) { %>
              <li class="nav-item active">
                <a class="nav-link" href="/signIn">login <span class="sr-only">(current)</span></a>
              </li>
              <li class="nav-item active">
                <a class="nav-link" href="/signUp">signUp <span class="sr-only">(current)</span></a>
              </li>
          <% } %>
          <ul>
    
    
            <ul class="navbar-nav mr-auto">
          <% if(isLoggedIn) { %>
              <li class="nav-item active">
                <form action="/logout" method="POST">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button type="submit" class="btn btn-outline-light" >logout</button>
                </form>
              </li>
          <% } %>
        </ul>

            <ul class="navbar-nav mr-auto">
          <% if(isLoggedIn) { %>
              <li class="nav-item active">
                <form action="/cart" method="GET">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button type="submit" class="btn btn-outline" ><i class="fa fa-shopping-cart"></i></button>
                </form>
              </li>
          <% } %>
        </ul>
        </div>

    </div>
</nav>
</html>